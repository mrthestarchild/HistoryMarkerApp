import { HistoryMarkerInfo } from './history-marker-info.model';

describe('HistoryMarkerInfo', () => {
  it('should create an instance', () => {
    expect(new HistoryMarkerInfo()).toBeTruthy();
  });
});
