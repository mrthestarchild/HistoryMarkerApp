import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.skylabs.landmark',
  appName: 'Landmark',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
